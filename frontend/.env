# Dev: leave API base empty to use Vite proxy (recommended — no CORS issues)
VITE_API_BASE_URL=

# Node API port (must match apis/.env PORT). Used by Vite proxy only.
VITE_API_PROXY_TARGET=http://localhost:6000

VITE_STORAGE_BASE_URL=http://localhost/p/public/storage/
VITE_API_TIMEOUT=30000
