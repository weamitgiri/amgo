import { Server, Socket } from 'socket.io';
/**
 * Socket.IO Handler for Game Activities and Real-time Communications
 */
export declare const setupSocketHandlers: (io: Server, socket: Socket) => void;
